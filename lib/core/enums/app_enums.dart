// Enum for card types
enum RestaurantsCardType { nearBy, allRestaurants, dineout }

enum DineOutCardType { common, poweredBy }

enum ChipDirection { row, column }

enum TextStyleType {
  // General Heading type
  heading,
  heading1,
  heading2,

  // General Subheading type
  subheading,
  subheading1,
  subheading2,

  // General Body type
  body,
  body1,
  body2,
}
